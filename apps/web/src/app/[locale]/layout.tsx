import '../globals.css';

// A request-scoped CSP nonce requires every localized document to render dynamically.
export const dynamic = 'force-dynamic';

export { default, generateMetadata } from './layout-workspace';
