{
  "name": "@cueq/database",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Prisma schema and database client for cueq",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "rm -rf dist && tsc --project tsconfig.build.json",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:push:force": "prisma db push --accept-data-loss",
    "db:migrate": "prisma migrate dev",
    "db:migrate:deploy": "prisma migrate deploy",
    "db:seed:baseline": "node prisma/seed-baseline.mjs seed",
    "db:reset:baseline": "node prisma/seed-baseline.mjs reset",
    "db:seed:demo": "node prisma/seed-demo.mjs seed",
    "db:reset:demo": "node prisma/seed-demo.mjs reset",
    "db:studio": "prisma studio",
    "clean": "rm -rf dist node_modules/.cache"
  },
  "dependencies": {
    "@prisma/client": "6.19.2"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "catalog:",
    "@types/node": "catalog:",
    "@typescript/native": "catalog:",
    "prisma": "6.19.2",
    "vitest": "catalog:"
  }
}
