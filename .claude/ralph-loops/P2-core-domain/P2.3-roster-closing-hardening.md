# P2.3: Roster & Closing Module Hardening

You are hardening two core domain modules. Work through items ONE AT A TIME.

### Roster (`packages/core/src/core/roster/`)

- Shift compliance validation, minimum staffing checks, plan-vs-actual comparison

Product spec: `docs/product-specs/roster-shift-planning.md`
Tests: `packages/core/src/core/roster/__tests__/`

### Closing (`packages/core/src/core/closing/`)

- Month-end checklist generation, cut-off lock transitions, export run lifecycle

Product spec: `docs/product-specs/monthly-closing.md`
Tests: `packages/core/src/core/closing/__tests__/`

### Audit (`packages/core/src/core/audit/`)

- Append-only audit entry builder with type-level immutability enforcement

Tests: `packages/core/src/core/audit/__tests__/`

## What To Do

For the roster module:

1. Test minimum staffing violations (0 assigned, exactly min, above min)
2. Test overlapping shifts for same person
3. Test shift type transitions (EARLY->LATE same day: rest period check)
4. Test plan-vs-actual when no bookings exist for a shift
5. Test roster status transitions (DRAFT->PUBLISHED->CLOSED)

For the closing module:

1. Test checklist generation with mixed states (some items resolved, some not)
2. Test lock transition guards: OPEN->REVIEW->CLOSED->EXPORTED
3. Test invalid transitions (EXPORTED->OPEN should fail)
4. Test export run idempotency (same period, same data = same checksum)
5. Test closing with pending workflow approvals (should block)

For the audit module:

1. Verify `buildAuditEntry` produces valid `AuditEntryDraft` for every action type
2. Verify the typecheck file catches mutations at compile time
3. Verify the compliance test enforces append-only semantics

## What NOT To Do

- Do NOT modify API or frontend code
- Do NOT change schemas or types (P1 handles that)
- Stay within `packages/core/src/core/{roster,closing,audit}/`

## Verification

`make test-unit && make test-compliance`

## Progress

Update `.claude/ralph-loops/P2-core-domain/P2.3-progress.md` after each item.

## Completion

<promise>P2.3-ROSTER-CLOSING-COMPLETE</promise>
