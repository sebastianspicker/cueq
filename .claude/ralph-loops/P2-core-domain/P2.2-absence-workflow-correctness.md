# P2.2: Absence & Workflow Correctness

You are hardening two core domain modules. Work through items ONE AT A TIME.

### Absence (`packages/core/src/core/absence/`)

- `prorating.ts` — pro-rata leave target calculation
- `working-days.ts` — working day counting (respects NRW holidays)
- `leave-ledger.ts` — leave balance tracking with carry-over and forfeiture
- `index.ts` — public API

Product spec: `docs/product-specs/absence-leave.md`
Existing tests: `packages/core/src/core/absence/__tests__/`
Holiday fixture: `fixtures/calendars/nrw-holidays-2026.json`

### Workflow (`packages/core/src/core/workflow/`)

- `index.ts` — approval state machine, delegation chain resolution, escalation

Product spec: `docs/product-specs/workflows-approvals.md`
Existing tests: `packages/core/src/core/workflow/__tests__/`

## What To Do

For the absence module:

1. Test pro-rata calculation for mid-year start/end dates
2. Test carry-over rules (TV-L: carry to March 31 of next year, then forfeit)
3. Test part-time employees (30h/week = proportional leave)
4. Test edge case: employee starts on a holiday, terminates on last working day
5. Test empty booking periods

For the workflow module:

1. Test every state transition in the FSM (Draft->Submitted->Pending->Approved/Rejected)
2. Test escalation: what happens when deadline passes?
3. Test delegation chains: A delegates to B, B delegates to C, C decides
4. Test circular delegation prevention
5. Test maxDelegationDepth enforcement
6. Test concurrent approval attempts (idempotency)

Fix any bugs discovered.

## What NOT To Do

- Do NOT modify API services, controllers, or frontend
- Do NOT change schemas (P1 handles that)
- Stay within `packages/core/src/core/absence/` and `packages/core/src/core/workflow/`

## Verification

`make test-unit`

## Progress

Update `.claude/ralph-loops/P2-core-domain/P2.2-progress.md` after each item.

## Completion

<promise>P2.2-ABSENCE-WORKFLOW-COMPLETE</promise>
