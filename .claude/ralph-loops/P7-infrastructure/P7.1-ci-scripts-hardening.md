# P7.1: CI & Scripts Hardening

You are auditing the CI pipeline and build scripts.
Work through items ONE AT A TIME.

CI workflows (3 files):

- `.github/workflows/ci.yml` — main CI: lint, typecheck, test, acceptance, a11y, build
- `.github/workflows/backup-restore-weekly.yml` — weekly backup/restore verification
- `.github/workflows/codeql.yml` — CodeQL analysis

Scripts (in `scripts/`):

- Shell scripts: check.sh, generate.sh, openapi-check.sh, pnpm.sh, schemas.sh, setup.sh, lib.sh
- Node scripts: validate-schemas.mjs, validate-fixtures.mjs, export-openapi.mjs,
  generate-core-schema-types.mjs, generate-db-schema-doc.mjs, check-doc-links.mjs,
  backup-restore-verify.mjs, hr-import.mjs, backfill-\*.mjs

## What To Do

1. CI workflow audit:
   - Are all third-party actions pinned to commit SHAs? (verify)
   - Is `permissions` properly scoped?
   - Does the CI matrix cover the right Node/pnpm versions?
   - Is cache usage optimal (pnpm store, turbo cache)?
   - Do failing steps produce actionable error messages?
   - Is there proper concurrency management (cancel-in-progress)?

2. Script robustness:
   - Do all shell scripts use `set -euo pipefail`?
   - Do scripts handle missing dependencies gracefully?
   - Are file paths quoted properly (spaces in paths)?
   - Do Node scripts have proper error handling?
   - Are there scripts that should be consolidated?

3. Build pipeline:
   - Does `make check` run all validation steps in the right order?
   - Does `make setup` work on a fresh clone? (validate by reading the script)
   - Does `make generate` produce deterministic output?
   - Is `make clean` thorough enough?

4. Fix issues found

## What NOT To Do

- Do NOT add deployment or CD automation
- Do NOT change source code or tests
- Do NOT modify schemas or domain logic
- Do NOT add new CI workflows (only fix existing)

## Verification

`make check`

## Progress

Update `.claude/ralph-loops/P7-infrastructure/P7.1-progress.md` after each item.

## Completion

<promise>P7.1-CI-SCRIPTS-COMPLETE</promise>
