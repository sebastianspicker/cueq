# P5.2: i18n & Component Consistency Audit

You are auditing the shared component library and internationalization setup.
Work through items ONE AT A TIME.

Components (11 files in `apps/web/src/components/`):
Button, ConnectionPanel, EmptyState, ErrorBoundary, FormField,
LoadingSpinner, PageShell, Pagination, SearchInput, SectionCard, StatusBanner

i18n files:

- `apps/web/src/messages/de.json`
- `apps/web/src/messages/en.json`
- `apps/web/src/i18n/request.ts`

API layer:

- `apps/web/src/lib/api-client.ts`
- `apps/web/src/lib/api-context.tsx`
- `apps/web/src/lib/use-api-query.ts`

## What To Do

1. Component audit:
   - Each component should accept only typed props (no `any`)
   - Components should be pure/presentational where possible
   - Check that components handle edge cases (empty lists, long text, null values)
   - Verify consistent styling patterns across all components
   - Check keyboard navigation support in interactive components
     (Button, Pagination, SearchInput)

2. i18n consistency:
   - Compare de.json and en.json key-by-key — every key in one must exist in the other
   - Check for placeholder values ("[TODO]", "...", untranslated German in en.json)
   - Verify interpolation patterns are consistent ({count}, {name} etc.)
   - Check that domain terms match the glossary in `docs/design-docs/core-beliefs.md`

3. API client:
   - Review `api-client.ts` for proper error handling
   - Verify the API context provides typed hooks, not raw fetch

4. Fix issues found

## What NOT To Do

- Do NOT modify backend API code
- Do NOT change schemas or core domain
- Do NOT add new components
- Do NOT install new packages

## Verification

`make typecheck && pnpm --filter @cueq/web test`

## Progress

Update `.claude/ralph-loops/P5-frontend/P5.2-progress.md` after each item.

## Completion

<promise>P5.2-I18N-COMPONENTS-COMPLETE</promise>
