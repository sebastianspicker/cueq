# P3.2 Progress: Policy Compliance & Integration Edge Cases

## Status: COMPLETE

## Item 1: Review policy.compliance.test.ts

Before: 1 test (TV-L baseline check only).

After: 10 tests across 3 describe blocks:

- GDPR absence reason privacy (2 tests): Verifies policy rule schemas don't include GDPR-sensitive fields and that PolicyViolation schema strips unknown top-level fields.
- Severity levels (3 tests): Validates the exact ERROR/WARNING/INFO enum, rejects invalid severities, documents ArbZG break/rest violations carry ERROR severity.
- Determinism (4 tests): Repeated getActivePolicyBundle calls produce identical results, bundle ordering is stable, schema parsing is deterministic, POLICY_HISTORY is frozen/immutable.

## Item 2: Review policy.acceptance.test.ts

Before: 1 test (on-call rest reduction only).

After: 14 tests across 7 describe blocks covering all reference calculation profiles:

- Flextime (flextime.json): 39.83h weekly target, 8.5h within extended max, 48h weekly max
- Shift/Pforte (pforte-shift.json): Night shift break calculation, cross-midnight handling, night window coverage
- Part-time change (part-time-change.json): Pro-rata on entry/exit, full-time to 30h ratio
- On-call/IT (it-oncall.json): 11h minimum rest after deployment, actual rest calculation (11.67h)
- Surcharge weekend/night overlap: HIGHEST_ONLY resolves to WEEKEND
- Surcharge holiday overlap: HIGHEST_ONLY resolves to HOLIDAY
- Fixture version alignment: All fixtures reference policyVersion 1

## Item 3: Review policy.integration.test.ts

Before: 3 tests (break ordering, bundle resolution, history filter).

After: 35 tests across 6 describe blocks:

- Original 3 tests preserved
- Bundle resolution at different dates (4 tests)
- Policy version transition (5 tests): v1 on Jan 31, v2 on Feb 1, no gap, overlap resolution
- Surcharge stacking (5 tests): Triple overlap, double overlap, night-only, unique priorities
- Break threshold boundaries (7 tests): Exact 6h/9h thresholds, just-under/just-over, between, below
- Leave pro-rata full-time to part-time (7 tests): 15 + 9 = 24 days, schema validation, carry-over

## Verification

pnpm --filter @cueq/policy test -> 4 files, 59 tests passed
pnpm --filter @cueq/policy test:unit -> 4 files, 59 tests passed
pnpm --filter @cueq/policy test:compliance -> 1 file, 10 tests passed

All tests green.
