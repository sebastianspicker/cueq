# P3.1: Policy Rule Coverage & Golden Tests

You are auditing the policy engine in `packages/policy/`. Work through items ONE AT A TIME.

Structure:

- `src/types.ts` — Zod schemas for PolicyRuleMeta, PolicyViolation, PolicyEvalResult
- `src/catalog.ts` — Policy history, version resolution, active bundle lookup
- `src/rules/break-rules.ts` — Break/pause enforcement (ArbZG: 30min after 6h, 45min after 9h)
- `src/rules/rest-rules.ts` — Minimum rest between shifts (11h per ArbZG)
- `src/rules/max-hours-rules.ts` — Daily/weekly max working hours
- `src/rules/leave-rules.ts` — Leave entitlement rules (TV-L: 30 days at 5-day week)
- `src/rules/surcharge-rules.ts` — Night/weekend/holiday surcharge rates

Tests:

- `src/__tests__/golden-cases.test.ts` — deterministic golden test cases
- `src/__tests__/reference-fixtures.test.ts` — tests against fixture files
- `src/__tests__/policy.compliance.test.ts` — compliance verification

Product spec: `docs/product-specs/policy-as-code.md`

## What To Do

1. Read each rule file and identify every code path
2. For each rule, verify golden tests exist for:
   - Happy path (no violation)
   - Exact boundary (e.g. exactly 6h worked = break check triggers)
   - Violation (clearly over limit)
   - Edge: part-time worker with different weekly hours
   - Edge: holiday falling on a weekend
   - Edge: overnight shift crossing midnight
3. Add missing golden test cases with exact expected outputs
4. Verify `getActivePolicyBundle()` correctly resolves version conflicts
   (latest version wins within effective date range)
5. Verify `getPolicyHistory()` filtering and sorting
6. Run the golden test suite after each change

## What NOT To Do

- Do NOT modify core domain logic in `packages/core/`
- Do NOT change API services or controllers
- Do NOT change schema files
- Stay within `packages/policy/`

## Verification

`pnpm --filter @cueq/policy test:golden && make test-unit`

## Progress

Update `.claude/ralph-loops/P3-policy-engine/P3.1-progress.md` after each item.

## Completion

<promise>P3.1-GOLDEN-TESTS-COMPLETE</promise>
