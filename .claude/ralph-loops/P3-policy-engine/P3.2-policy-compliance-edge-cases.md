# P3.2: Policy Compliance & Integration Edge Cases

You are verifying that the policy engine handles compliance-critical edge cases.
Work through items ONE AT A TIME.

## What To Do

1. Review `packages/policy/src/__tests__/policy.compliance.test.ts`:
   - Does it test that GDPR-relevant fields (absence reasons) are never exposed
     to unauthorized roles?
   - Does it test that policy violations include correct severity levels?
   - Does it test that policy evaluation results are deterministic
     (same input = same output, always)?

2. Review `packages/policy/src/__tests__/policy.acceptance.test.ts`:
   - Does it cover the reference calculation profiles?
     (flextime, shift/Pforte, part-time change, on-call/IT)
   - Does it match the fixtures in `fixtures/reference-calculations/`?

3. Review `packages/policy/src/__tests__/policy.integration.test.ts`:
   - Does it test policy bundle resolution at different dates?
   - Does it test what happens when no policy is active for a date?

4. Add missing tests for:
   - Policy version transition (old rule active until Jan 31, new rule from Feb 1)
   - Multiple surcharge categories stacking (Sunday + holiday + night)
   - Break rules for shifts exactly at threshold boundaries
   - Leave pro-rata for an employee who changes from full-time to part-time mid-year

## What NOT To Do

- Do NOT modify core domain logic
- Do NOT change API or frontend code
- Stay within `packages/policy/`

## Verification

`make test-unit && make test-compliance`

## Progress

Update `.claude/ralph-loops/P3-policy-engine/P3.2-progress.md` after each item.

## Completion

<promise>P3.2-POLICY-COMPLIANCE-COMPLETE</promise>
