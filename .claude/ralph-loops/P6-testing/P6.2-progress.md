# P6.2 Edge Case Tests — Progress

## Item 1: Acceptance Test Assertions ✅

Strengthened assertions in `apps/api/test/acceptance/phase2.acceptance.test.ts`:

- **AT-01**: Added `batchId` presence check, `created` count assertion, and batch detail retrieval verification
- **AT-05**: Added non-compliant path test (insufficient rest window returns `compliant: false`)
- **AT-06**: Added CSV data row count assertion (not just header check)
- **AT-07**: Added lead-specific absence detail visibility assertion; fixed query param format (date, not datetime)

## Item 2: Integration Tests for Untested Endpoints ✅

Created `apps/api/test/integration/edge-cases.integration.test.ts` (10 tests):

- **GET /v1/rosters/current**: Happy-path test using seeded PUBLISHED roster, 404 test for no active roster
- **POST /v1/closing-periods/{id}/start-review**: Happy-path (admin + env enabled), disabled test, non-admin rejection
- **GET /v1/integrations/webhooks/endpoints**: Happy-path list test for HR
- **GET /v1/absences/me**: Empty result edge case
- **GET /v1/bookings/me**: Empty result edge case
- **GET /v1/closing-periods**: Role-based list for HR and team lead

## Item 3: GDPR Compliance Tests ✅

Created `apps/api/test/compliance/gdpr-edge-cases.compliance.test.ts` (13 tests, all passing):

- **Absence reason visibility**: Employee sees only APPROVED + ABSENT (no type/note), lead sees type + REQUESTED
- **Audit trail immutability**: No `updatedAt` column, entries preserve data integrity, every booking produces audit
- **Data minimization**: audit-summary no actor IDs, compliance-summary aggregates only, custom preview no person IDs
- **Report access audit logging**: audit-summary and compliance-summary log REPORT_ACCESSED entries

## Item 4: Terminal Gateway Edge Cases ✅

Created `apps/api/test/integration/terminal-edge-cases.integration.test.ts` (15 tests):

- **Duplicate handling**: Intra-batch dedup (3 identical → 2 dupes), non-duplicate detection, cross-batch dedup
- **Malformed CSV**: Header-only CSV, empty body, missing columns, invalid dates, extra columns, mixed rows
- **Extended offline sync**: Old timestamps sync, heartbeat with lastErrorCount, large batch (50 records)
- **Absence conflict flags**: Booking overlapping an approved absence produces ABSENCE_CONFLICT
- **Access control**: Employee rejection for batch and file sync

## Fixes Applied

- **AT-07 date format**: Team calendar query uses `YYYY-MM-DD` (not ISO datetime) per `DateSchema`
- **Roster current test**: Uses seeded PUBLISHED roster directly instead of API creation (avoids OU overlap + role scoping)
- **Heartbeat health test**: Checks `lastErrorCount` (not `bufferedRecords`) matching health endpoint response shape
- **Conflict flags test**: Tests ABSENCE_CONFLICT (approved absence overlap), not booking time overlap (which is normal behavior)

## Verification ✅

| Suite             | Result                                     |
| ----------------- | ------------------------------------------ |
| Unit tests        | 89/89 passed                               |
| Core domain       | 387/387 passed                             |
| Integration tests | 147/147 passed                             |
| Compliance tests  | 28/28 passed (including 13 new GDPR tests) |
| Acceptance tests  | 8/8 passed (AT-01 through AT-08)           |
| TypeScript        | Clean (no errors)                          |
| **Total**         | **659/659 passed**                         |
