# P6.2: Missing Edge Case Tests

You are adding edge case tests discovered in P6.1's coverage gap analysis.
Work through items ONE AT A TIME.

Read the gap report at `.claude/ralph-loops/P6-testing/coverage-gaps.md`.

Focus on integration and acceptance tests that exercise the full stack
(API -> service -> domain -> database).

## What To Do

1. Review acceptance test scenarios from `docs/QUALITY_SCORE.md` (AT-01 through AT-08).
   For each:
   - Read the test file
   - Does it actually assert meaningful outcomes? Or just "doesn't throw"?
   - Add missing assertions

2. Add integration tests for untested API endpoints:
   - Compare the OpenAPI spec (from `contracts/`) against test files
   - Every endpoint should have at least: happy path, 400 (bad input), 403 (wrong role)

3. Add compliance tests for untested GDPR requirements:
   - Absence reason visibility (only authorized roles see reasons)
   - Audit trail immutability (attempt to update/delete audit entries)
   - Data minimization in report outputs

4. Add edge case tests for the terminal gateway:
   - Duplicate record handling in batch sync
   - Malformed CSV input
   - Terminal offline for extended period then sync

## What NOT To Do

- Do NOT modify source code (tests only)
- Do NOT change schemas or core domain logic
- Do NOT add new features

## Verification

`make test-all`

## Progress

Update `.claude/ralph-loops/P6-testing/P6.2-progress.md` after each item.

## Completion

<promise>P6.2-EDGE-CASE-TESTS-COMPLETE</promise>
