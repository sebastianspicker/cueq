# P1.3: Type Generation Consistency

You are verifying the type generation pipeline. Work through items ONE AT A TIME.

The generation script is `scripts/generate-core-schema-types.mjs`.
Generated types land in `packages/shared/src/generated/core-schema-types.ts`.
They're re-exported from `packages/shared/src/index.ts`.

## What To Do

1. Read `scripts/generate-core-schema-types.mjs` and understand what it generates
2. Run `make generate` and diff the output against the committed generated files
3. Verify every JSON Schema in `schemas/domain/` has a corresponding generated type
4. Check that `packages/shared/src/index.ts` re-exports all generated types
5. Verify no hand-written types in `packages/shared/src/schemas/*.ts` duplicate
   what should come from generation (ESLint rule `cueq/no-manual-schema-types`)
6. Check that `packages/core/src/core/types.ts` doesn't redefine types that exist
   in shared -- it should import them or define only core-internal types
7. Verify the `make generate` -> `make typecheck` pipeline is clean

## What NOT To Do

- Do NOT modify business logic
- Do NOT touch test files except to fix import paths
- Do NOT change JSON Schemas (that was P1.1)
- Do NOT add new schemas or types

## Verification

`make generate && make typecheck && make schemas`

## Progress

Update `.claude/ralph-loops/P1-foundation/P1.3-progress.md` after each item.

## Completion

<promise>P1.3-TYPE-GENERATION-COMPLETE</promise>
