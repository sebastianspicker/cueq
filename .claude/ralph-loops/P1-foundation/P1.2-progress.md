# P1.2: Database Model Audit — Progress

**Status:** COMPLETE
**Date:** 2026-03-22

---

## 1. Indexing Audit

### Added Missing FK Indexes (schema.prisma)

| Model            | Index Added                                  | Reason                                               |
| ---------------- | -------------------------------------------- | ---------------------------------------------------- |
| Person           | `@@index([organizationUnitId])`              | FK to OrganizationUnit, no implicit PG index         |
| Person           | `@@index([supervisorId])`                    | FK self-relation, frequent supervisor lookups        |
| Booking          | `@@index([typeTypeId])`                      | FK to TimeType, JOIN queries for time-type filtering |
| Roster           | `@@index([organizationUnitId, periodStart])` | Composite for OU + date range roster lookups         |
| Shift            | `@@index([rosterId])`                        | FK to Roster, loading shifts for a roster            |
| OnCallDeployment | `@@index([rotationId])`                      | FK to OnCallRotation, deployment lookups             |
| ExportRun        | `@@index([closingPeriodId])`                 | FK to ClosingPeriod, export history                  |

### AuditEntry Immutability: CONFIRMED

- No updatedAt field, no FK relations, no cascade delete possible

---

## 2. Enum Consistency Audit

11 of 12 enums match perfectly between Prisma and shared/core.

### Known Divergence: ClosingStatus

- Prisma: OPEN, REVIEW, **CLOSED**, EXPORTED
- Core/Shared: OPEN, REVIEW, **APPROVED**, EXPORTED
- Intentionally handled by adapter functions in API layer
- Recommendation: align in future coordinated PR

### WorkTimeModelType: Not mirrored in shared (no frontend need yet)

---

## 3. Referential Integrity: ALL CORRECT

- AuditEntry has no relations (cannot cascade delete)
- Prisma defaults to Restrict for required relations (correct)
- Deprecated Shift.personId still actively used in API layer (document for future removal)

---

## 4. Constraints

- Added @db.Decimal(10, 2) to 8 hours/days fields
- All existing @@unique constraints verified correct

---

## 5. Tests Enhanced

- Added 3 new compliance tests for AuditEntry schema validation and Decimal field checks
- All 140 tests pass, typecheck clean

---

## Changes Summary

- schema.prisma: +15 lines (7 indexes, 8 Decimal precisions)
- database.compliance.test.ts: +35 lines (3 new test cases)
- Total: ~50 lines changed
