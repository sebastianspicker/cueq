# P1.3 Type Generation Consistency — Progress

## Status: COMPLETE

No fixes needed. The type generation pipeline is consistent and clean.

## Verification Results

### 1. Generation Script Understanding

- `scripts/generate-core-schema-types.mjs` reads `schemas/domain/core-*.schema.json`
- Generates TypeScript types using schema `title` as type name
- Output: `packages/shared/src/generated/core-schema-types.ts`
- Handles `$ref`, enums, arrays, objects, union types, optionality

### 2. Generate + Diff

- Ran `node scripts/generate-core-schema-types.mjs` and compared output
- Only differences: double vs single quotes, multi-line formatting of unions
- After Prettier formatting: **identical** (excluding timestamp line)
- Pipeline: generate → Prettier → commit produces consistent output

### 3. Schema-to-Type Coverage (8/8)

| Schema File                           | Generated Type                  |
| ------------------------------------- | ------------------------------- |
| core-audit-entry-draft.schema.json    | CoreAuditEntryDraftContract     |
| core-closing.schema.json              | CoreClosingContract             |
| core-flextime-calculation.schema.json | CoreFlextimeCalculationContract |
| core-oncall-rest.schema.json          | CoreOnCallRestContract          |
| core-prorated-target.schema.json      | CoreProratedTargetContract      |
| core-shift-compliance.schema.json     | CoreShiftComplianceContract     |
| core-time-rule-evaluation.schema.json | CoreTimeRuleEvaluationContract  |
| core-workflow-transition.schema.json  | CoreWorkflowTransitionContract  |

### 4. Re-exports

- `packages/shared/src/index.ts` line 22: `export * from './generated/core-schema-types'`
- Wildcard re-export covers all 8 types

### 5. No Duplicate Hand-Written Types

- No `Core*Contract` types found in `packages/shared/src/schemas/*.ts`
- `time-engine.ts` has `TimeRuleEvaluationRequest/Response` (Zod-derived) — complementary API-layer types, not duplicates of the generated contract type
- `common.ts` has one ESLint disable for `PaginatedResponse<T>` — legitimate generic type that can't be expressed as JSON Schema
- ESLint rule `cueq/no-manual-schema-types` is configured as `'error'`

### 6. Core Types — No Redefinitions

- `packages/core/src/core/types.ts` defines richer domain types:
  - `RuleViolation` (with `ViolationCode | (string & {})` branded union)
  - `DomainWarning`, `AuditEntryDraft`, `ChecklistItem`, etc.
- These are core-internal types, **not** duplicates of generated contracts
- Core correctly imports `Core*Contract` types from `@cueq/shared`
- Core never imports `RuleViolation`/`DomainWarning` from shared — uses its own richer versions
- `packages/core/src/core/time-engine/types.ts` only defines `TimeEnginePolicy` (core-internal)

### 7. Full Pipeline

```
make generate  -> Prisma, OpenAPI, DB docs, core types all generated successfully
make typecheck -> 10/10 tasks pass (6 packages)
make schemas   -> 32 domain schemas compiled, 11 fixtures validated
```

## Architecture Summary

```
schemas/domain/core-*.schema.json   (source of truth)
        ↓  generate-core-schema-types.mjs
packages/shared/src/generated/core-schema-types.ts  (contract boundary types)
        ↓  re-exported via index.ts
packages/core/src/  (imports Core*Contract for function signatures)
packages/core/src/core/types.ts  (richer internal domain types — complementary)
packages/shared/src/schemas/*.ts  (Zod schemas for API validation — complementary)
```
