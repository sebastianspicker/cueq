# P4.1 — Service Decomposition Quality Audit

## Status: COMPLETE (verified 2026-03-22)

All oversized services decomposed to <400 lines. All 24 helpers <400 lines (largest: workflow-assignment.helper at 378).
Services act as thin orchestrators delegating to focused helpers via NestJS DI.
Public API (method signatures) unchanged — controllers unmodified.

## Results

| Service                     | Before | After | Extracted Helpers                                                                       |
| --------------------------- | ------ | ----- | --------------------------------------------------------------------------------------- |
| closing-domain.service.ts   | 1368   | 223   | closing-utils, closing-export, closing-checklist, closing-correction, closing-lifecycle |
| workflow-runtime.service.ts | 964    | 330   | workflow-utils, workflow-assignment, workflow-delegation-crud, workflow-side-effects    |
| roster-domain.service.ts    | 921    | 284   | roster-shift, roster-assignment, roster-query                                           |
| workflows-domain.service.ts | 697    | 181   | workflow-creation, workflow-side-effects                                                |
| reporting.service.ts        | 567    | 153   | reporting-compliance, reporting-analytics                                               |
| absence-domain.service.ts   | 492    | 359   | leave-balance                                                                           |
| terminal-gateway.service.ts | 376    | 376   | (already under limit)                                                                   |

## Quality Audit Fixes (2026-03-22)

1. **Consolidated triplicated roster guards**: `assertCanWriteRoster` and `assertRosterIsDraft` were duplicated across `RosterDomainService`, `RosterShiftHelper`, and `RosterAssignmentHelper`. Consolidated into `RosterShiftHelper` as public methods; other classes delegate.

2. **Consolidated duplicated `minGroupSize()`**: Was duplicated in `ReportingService` and `ReportingComplianceHelper`. Made public on `ReportingComplianceHelper`; service delegates.

3. **Extracted reporting analytics**: `reportTeamAbsence`, `reportOeOvertime`, `reportClosingCompletion` were inline in `ReportingService` (336 lines). Extracted to `ReportingAnalyticsHelper` (210 lines). Service is now 153 lines — a true thin orchestrator. `reportCustomPreview` calls helpers directly instead of self-calling.

4. **Moved workflow-runtime inline business logic to helpers**:
   - Self-approval guard for `POST_CLOSE_CORRECTION` moved to `WorkflowSideEffectsHelper.validatePostCloseSelfApproval()`.
   - Inline delegation validation moved to `WorkflowDelegationCrudHelper.validateInlineDelegation()`.
   - `workflow-runtime.service.ts` reduced from 369 to 330 lines.

## Verification

make typecheck -> PASS (10/10 turbo tasks)
make test-unit -> PASS (487 tests across 6 packages)
make test-integration -> PASS (89 tests across 8 test files)
