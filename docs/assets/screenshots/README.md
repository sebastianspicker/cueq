# Alpha screenshots

The images under `alpha/` are static, sanitized product examples. They are not
evidence of API, database, identity-provider, browser-automation, or production
behavior.

Review their synthetic-only data and visible role labels manually before a
publication. The repository does not generate or validate browser screenshots.
